name: dex_wallet
description: A premium non-custodial EVM wallet (initial architecture)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  flutter_riverpod: ^2.3.6
  go_router: ^7.4.0
  dio: ^5.1.2
  flutter_secure_storage: ^8.0.0
  bip39: ^2.0.5
  bip32: ^0.1.6
  web3dart: ^2.6.0
  convert: ^3.1.0
  local_auth: ^2.1.5
  crypto: ^3.0.2
  json_annotation: ^4.8.1
  web_socket_channel: ^2.2.0
  url_launcher: ^6.1.7
  walletconnect_dart: ^1.0.0
  flutter_dotenv: ^5.0.2
  walletconnect_flutter_v2: ^0.1.0
  qr_flutter: ^4.0.0
  intl: ^0.18.1
  mobile_scanner: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  freezed: ^2.2.0
  json_serializable: ^6.6.1
  mocktail: ^0.3.0
